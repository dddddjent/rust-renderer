[package]
name = "renderer"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
simple-math = { path = "../simple-math" }
renderer-derive = {path = "renderer-derive"}

serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
log = "0.4"
simplelog = "^0.12.0"
image = "0.24.7"
rand = "0.8.5"
